<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="exam.projects.sosu_final">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_configuration"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight"
        tools:targetApi="31">
        <activity
            android:name=".activities.functionability.FunctionAbilityActivity"
            android:exported="false" />
        <activity
            android:name=".activities.healthcondition.SingleHealthConditionActivity"
            android:exported="false" />
        <activity
            android:name=".activities.healthcondition.HealthConditionActivity"
            android:exported="false" />
        <activity
            android:name=".activities.AddSubjectActivity"
            android:exported="false" />
        <activity
            android:name=".activities.generalinformation.GeneralInformationActivity"
            android:exported="false" />
        <activity
            android:name=".activities.SubjectActivity"
            android:exported="false" />
        <activity
            android:name=".activities.DashboardActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>